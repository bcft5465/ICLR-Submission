# SAFER Benchmark — Python dependencies
# Tested on: Python 3.11, NVIDIA GPU (sm_120), CUDA 12.8
#
# INSTALL ORDER MATTERS:
#   1. torch (pinned — vLLM 0.19.0 requires torch==2.10.0 exactly)
#   2. vllm  (pinned — must match torch)
#   3. everything else
#
# Step 1 — PyTorch with CUDA 12.8 / Blackwell support:
#   pip install torch==2.10.0 torchvision==0.25.0 torchaudio==2.10.0 \
#       --index-url https://download.pytorch.org/whl/cu128
#
# Step 2 — vLLM:
#   pip install vllm==0.19.0
#
# Step 3 — everything else:
#   pip install -r requirements.txt
#
# DO NOT run `pip install open-flamingo` — it downgrades torch to 2.0.1.

# ── Core pipeline ────────────────────────────────────────────────────────────
pandas==3.0.3
numpy==2.2.6
PyYAML==6.0.3
scikit-learn==1.9.0
scipy==1.17.1
matplotlib==3.11.0
tqdm==4.68.2
pillow==12.2.0

# ── HuggingFace ecosystem ────────────────────────────────────────────────────
transformers==4.56.0
huggingface_hub==0.36.2
accelerate==1.13.0
tokenizers==0.22.2
safetensors==0.8.0
datasets==5.0.0
sentencepiece==0.1.98
timm==1.0.28

# ── BiomedCLIP loader ────────────────────────────────────────────────────────
open-clip-torch

# ── Utilities ────────────────────────────────────────────────────────────────
kaggle==2.2.4
setproctitle==1.3.7
einops==0.8.2
regex==2026.5.9
requests==2.34.2
filelock==3.29.0
packaging==26.0
psutil==7.2.2